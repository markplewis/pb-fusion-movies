{
  "name": "fusion-movies",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "fusion": "fusion",
    "start": "fusion start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "npx npm-check -s -u",
    "reinstall": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@arc-fusion/cli": "^1.0.7"
  },
  "dependencies": {
    "tgam-patterns": "^4.4.21"
  }
}
